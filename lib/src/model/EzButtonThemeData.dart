import 'package:flutter/material.dart';

class EzButtonThemeData {
  Color buttonColor;
  Color focusColor;
  Color highlightColor;
  Color hoverColor;
  Color splashColor;
  Color disabledColor;

  EzButtonThemeData(
      {this.buttonColor,
      this.disabledColor,
      this.focusColor,
      this.highlightColor,
      this.hoverColor,
      this.splashColor});
}
